spring:
  application:
    name: shopping-service
  cloud:
    config:
      uri: http://localhost:8091
      username: root
      password: s3cr3t
    bootstrap:
      enabled: 'true'
  boot:
    admin:
      client:
        url: http://localhost:8086